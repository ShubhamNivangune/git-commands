
# Hi, I'm Shubham! 👋


## Git Commands

```bash
  git init
```
```bash
  git branch <Branch Name>
```
```bash
  git checkout -b <Branch Name>
```
```bash
  git log
```
```bash
  git status
```
```bash
  git add <Branch Name>
```
```bash
  git commit -m "add message"
```
```bash
  git branch -a
```
```bash
  git merge <Branch Name> 
```
```bash
  git branch -d <Branch Name> 
```
```bash
  git --help 
```
### GitHub
…or create a new repository on the command line
```bash
  echo "# Git-Commands" >> README.md
  git init
  git add README.md
  git commit -m "first commit"
  git branch -M main
  git remote add origin https://github.com/<ADD Your Link>
  git push -u origin main
```
…or push an existing repository from the command line
```bash
  git remote add origin https://github.com/<ADD Your Link>
  git branch -M main
  git push -u origin main
```
## Authors

- [@ShubhamNivangune](https://github.com/ShubhamNivangune)

